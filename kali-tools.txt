# Kali Tool List
A collections of optional tools installation.

## apt and apt-get install
sudo apt update
sudo apt install gedit

# ssh
sudo systemctl start ssh
sudo systemctl enable ssh

#apache2
sudo systemctl start apache2
sudo systemctl enable apache2

sudo apt-get install python-dev build-essential python-setuptools virtualenvwrapper python-pip python3-pip
sudo apt-get install libwww-perl libdbd-sqlite3-perl libhtml-linkextractor-perl libterm-readline-gnu-perl liblwp-protocol-socks-perl sqlite3 libswitch-perl
sudo apt-get install -y libssl-dev libffi-dev python-dev build-essential python3-venv
sudo apt-get install python3-paramiko
sudo apt-get install -y xclip
sudo apt install git tor telnet
sudo apt install libpango1.0-0
sudo apt install nfs-common net-tools ncat
sudo apt install perl-doc

### Incase nikto is missing in Kali
sudo apt install nikto

### Empire framework
sudo apt install powershell-empire

### Domain sublister
sudo apt install sublist3r

### Screenshot grabbing tool, pass the output from sublist3r to eyewitness
sudo apt install eyewitness

###
sudo apt-get install nis

### MySQL injection and takeover tool
sudo apt-get install sqlsus

###
sudo apt install knock

### PIVOTING  Sshuttle
sudo apt install sshuttle

###
sudo apt install dnsrecon

###
sudo apt install dos2unix

### Openvpn
sudo apt install openvpn
sudo apt install john

### Drupal scanning
sudo apt install droopescan

### Joomla scanning
sudo apt install joomscan

### Gospider
sudo apt install gospider

sudo apt install ffuf

### MSFvenom Payload Creator
sudo apt install -y msfpc

usage - msfpc.sh <TYPE> (<DOMAIN/IP>) (<PORT>) (<CMD/MSF>) (<BIND/REVERSE>) (<STAGED/STAGELESS>) (<TCP/HTTP/HTTPS/FIND_PORT>) (<BATCH/LOOP>) (<VERBOSE>)

## python and pip

python -m pip -V
python3 -m pip -V
python3 -m pip install --user pipx
pip install progress
pip install raccoon-scanner
pipx install witnessme
pip install impacket

### Pwntools
sudo apt-get update
sudo apt-get install python3 python3-pip python3-dev git libssl-dev libffi-dev build-essential
python3 -m pip install --upgrade pip
python3 -m pip install --upgrade pwntools

### Inatall requests (check which works for you)
python3 -m pip install requests
pip install requests 
pip3 install requests
easy_install -U requests

### GitHub tools
sudo apt install git

### Impacket package
git clone https://github.com/SecureAuthCorp/impacket

git clone https://github.com/joxeankoret/CVE-2017-7494
git clone https://github.com/andyacer/ms08_067.git
git clone https://github.com/3ndG4me/AutoBlue-MS17-010
git clone https://github.com/trailofbits/winchecksec.git
git clone https://github.com/corelan/mona

## nmapAutomator
wget https://raw.githubusercontent.com/21y4d/nmapAutomator/master/nmapAutomator.sh && chmod 755 nmapAutomator.sh

wget https://raw.githubusercontent.com/Ogglas/Orignal-Slowloris-HTTP-DoS/master/slowloris.pl && chmod 755 slowloris.pl
wget https://raw.githubusercontent.com/brokensound77/OptionsBleed-POC-Scanner/master/bleeder.py && chmod 755 bleeder.py

wget https://raw.githubusercontent.com/BuddhaLabs/PacketStorm-Exploits/master/0304-exploits/ptrace-kmod.c

### Corelan scripts
wget https://raw.githubusercontent.com/corelan/Scripts/master/winappdbgtest.py && chmod 755
wget https://raw.githubusercontent.com/corelan/Scripts/master/hex2bin.py && chmod 755
wget https://raw.githubusercontent.com/corelan/Scripts/master/bin2hex.py && chmod 755

### Suid checker
wget https://raw.githubusercontent.com/Anon-Exploiter/SUID3NUM/master/suid3num.py --no-check-certificate && chmod 755 suid3num.py

### Git finder, extractor and dumper
wget https://raw.githubusercontent.com/internetwache/GitTools/master/Finder/gitfinder.py && chmod 755 gitfinder.py
wget https://raw.githubusercontent.com/internetwache/GitTools/master/Extractor/extractor.sh && chmod 755 extractor.sh
wget https://raw.githubusercontent.com/internetwache/GitTools/master/Dumper/gitdumper.sh && chmod 755 gitdumper.sh


### torghost
git clone https://github.com/SusmithKrishnan/torghost.git
cd torghost/
chmod +x install.sh
chmod +x build.sh
./build.sh

usage:
sudo torghost -s
curl https://ipinfo.io/json

### Kadimus
git clone https://github.com/P0cL4bs/Kadimus.git
wget https://raw.githubusercontent.com/SnollyG0st3r/Kadimus/master/configure && chmod 755 configure
./configure

!-- sudo apt-get install libcurl libpcre libssh
!-- sudo apt install libcurl libpcre libssh

### CrackMapExec CME installations
sudo apt-get install -y libssl-dev libffi-dev python-dev build-essential
sudo apt-get install virtualenvwrapper
source /usr/share/virtualenvwrapper/virtualenvwrapper.sh
mkvirtualenv CME
(CME) pip install crackmapexec

Usage AD Dump Creds Post exploit
crackmapexec .py -u <_Domain Admin> -p <_Domain Admin> -d <_Domain> <_AD IP Address> --ntds drsuapi

### Joplin note keeper app
wget -O - https://raw.githubusercontent.com/laurent22/joplin/dev/Joplin_install_and_update.sh | bash

### Reverse shells 
wget https://raw.githubusercontent.com/flozz/p0wny-shell/master/shell.php

### Dropbox
cd /home/kali
cd Download
wget https://www.dropbox.com/download?dl=packages/ubuntu/dropbox_2020.03.04_amd64.deb
sudo dpkg -i dropbox_2020.03.04_amd64.deb 
cd /home/kali
ln -s ~/Dropbox/<_Name of the Folder> ~/data

### python command for bash
python -c 'import pty; pty.spawn("/bin/bash")'
python -c 'import pty; pty.spawn("/bin/sh")'


### to get nobody removed in NFS share
sudo mount -t nfs -o nolock,nfsvers=3 10.1.1.5:/home /tmp/home/


### msf meterpreter sheel listener
msf > use exploit/multi/handler
msf > set payload windows/meterpreter/reverse_tcp
msf > set payload windows/shell/reverse_tcp


### Search
grep -rn './' -ie 'whatever' -B 2 -A 2
grep -rn './' -ie 'password' -B 2 -A 2

!--End --
